<Window x:Class="PoolInformationSystem.WindowClient.GuestOrderWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Оформление пробного абонемента" Height="650" Width="550"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize" Background="#f5f9fc">
    <Border Margin="20" Background="White" CornerRadius="10" Padding="25" BorderThickness="1" BorderBrush="#D3D3D3">
        <StackPanel>
            <TextBlock Text="ОФОРМЛЕНИЕ ПРОБНОГО АБОНЕМЕНТА" FontSize="22" FontWeight="Bold" Foreground="#b4d5e8" HorizontalAlignment="Center" Margin="0,0,0,25"/>

            <Border Background="#f0f7fb" CornerRadius="8" Padding="20" Margin="0,0,0,20">
                <StackPanel>
                    <TextBlock Text="Выбранный абонемент" FontSize="16" FontWeight="Bold" Foreground="#333" Margin="0,0,0,10"/>
                    <TextBlock x:Name="TbSelectedName" Text="-" FontSize="18" FontWeight="SemiBold" Foreground="#333" Margin="0,0,0,15"/>
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,5">
                        <TextBlock Text="Стоимость:" Width="150" Foreground="#808080"/>
                        <TextBlock x:Name="TbTotalPrice" Text="0 ₽" FontSize="22" FontWeight="Bold" Foreground="#b4d5e8"/>
                    </StackPanel>
                </StackPanel>
            </Border>

            <StackPanel Margin="0,0,0,25">
                <TextBlock Text="Способ оплаты *" Foreground="#808080" Margin="0,0,0,8"/>
                <Border BorderBrush="#D3D3D3" BorderThickness="1" CornerRadius="4" Background="White">
                    <ComboBox x:Name="CbPaymentMethod" FontSize="16" Padding="10,8" 
                              DisplayMemberPath="MethodName" SelectedValuePath="MethodId" SelectionChanged="CbPaymentMethod_SelectionChanged"/>
                </Border>
            </StackPanel>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <Button Content="ПОДТВЕРДИТЬ И ОПЛАТИТЬ" Background="#b4d5e8" Foreground="White" FontSize="15" FontWeight="Bold" Padding="30,10" Click="BtnConfirm_Click" Margin="0,0,15,0"/>
                <Button Content="ОТМЕНА" Background="LightGray" Foreground="Black" FontSize="15" FontWeight="Bold" Padding="30,10" Click="BtnCancel_Click"/>
            </StackPanel>
        </StackPanel>
    </Border>
</Window>